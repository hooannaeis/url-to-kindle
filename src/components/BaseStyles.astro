<style is:global>
  @import "tailwindcss";

  h1 {
    @apply text-2xl font-semibold text-center my-4 text-slate-50;
  }

  h2 {
    @apply text-xl font-semibold text-center my-4 text-slate-50;
  }

  h3 {
    @apply font-semibold text-center my-4 text-slate-50;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .spinner {
    display: inline-block;
    width: 16px;
    height: 16px;
    border: 2px solid rgba(0, 0, 0, 0.1);
    border-left-color: currentColor;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
  }
</style>
